# Exemplo de variáveis de ambiente para Render
# Não é necessário preencher - o Render configura automaticamente

# Porta do servidor (Render define automaticamente)
PORT=8080

# Ambiente
NODE_ENV=production

# WebSocket config
WS_PORT=8080
WS_HOST=0.0.0.0