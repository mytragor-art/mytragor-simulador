# Configuração do Render para MyTragor Simulador
services:
  - type: web
    name: mytragor-simulador
    env: node
    buildCommand: npm install
    startCommand: node server-render-otimizado.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000